<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.dao.RegisteredDao">
    <select id="selectRegisteredCount" resultType="com.example.entity.RegisteredDoctorDept">
        SELECT dept_id, doctor_id, dept_name, dept_location, dept_telephone, COUNT(*) AS patient_count
        FROM tb_registered
                 LEFT JOIN tb_work USING (doctor_id)
                 LEFT JOIN tb_department USING (dept_id)
        GROUP BY dept_name
    </select>
</mapper>